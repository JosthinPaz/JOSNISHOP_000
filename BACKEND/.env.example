# Example environment variables (DO NOT commit real secrets)
# Copy this to `.env` and fill with real values before running.

# SQLAlchemy DB URL example (MySQL / MariaDB):
# Format: mysql+pymysql://<DB_USER>:<DB_PASSWORD>@<DB_HOST>:<DB_PORT>/<DB_NAME>
SQLALCHEMY_DATABASE_URL=mysql+pymysql://user:password@127.0.0.1:3306/josnishop

# Strong JWT secret key for signing tokens
JWT_SECRET_KEY=replace-with-a-strong-secret

# Frontend: API base URL (used by Vite: must start with http(s)://)
VITE_API_URL=http://127.0.0.1:8000
