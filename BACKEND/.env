# ============================================
# VARIABLES DE ENTORNO - JOSNISHOP
# ============================================
# Copiar este archivo como .env y llenar con valores reales
# NUNCA commitear .env con valores reales al repositorio

# --- CORREO (CRÍTICO) ---
# Si SENDGRID_API_KEY está presente, se usa SendGrid. Si no, se usa el SMTP de abajo (que falla en Railway).
SMTP_EMAIL=josnishop@gmail.com
SMTP_PASSWORD=wssr opok igoz axjn
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=465

# --- SENDGRID (CRÍTICO para Railway/Producción) ---
# **REEMPLAZA EL VALOR ABAJO CON TU CLAVE REAL DE SENDGRID (EMPIEZA CON SG.)**
SENDGRID_API_KEY=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

# --- BD ---

# --- JWT ---
SECRET_KEY=josnishop_super_secreta_2024_cambiar
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30
REFRESH_TOKEN_EXPIRE_DAYS=7

# --- CORS ---
FRONTEND_URL=http://localhost:5173
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000,https://josnishop000-frontend-production.up.railway.app

# --- SEGURIDAD ---
ENVIRONMENT=development
DEBUG=False

# --- LIMITES DE ARCHIVOS ---
MAX_LOGIN_ATTEMPTS=5
LOGIN_ATTEMPT_TIMEOUT=300
MAX_FILE_SIZE_MB=5
MAX_VIDEO_DURATION_SECONDS=60

# --- RATE LIMITING (General) ---
RATE_LIMIT_MAX_REQUESTS=100
RATE_LIMIT_WINDOW_SECONDS=60

# --- RATE LIMITING (Por endpoint) ---
RATE_LIMIT_LOGIN_MAX_ATTEMPTS=5
RATE_LIMIT_LOGIN_WINDOW=300
RATE_LIMIT_REGISTER_MAX_ATTEMPTS=3
RATE_LIMIT_REGISTER_WINDOW=3600
RATE_LIMIT_API_MAX_REQUESTS=100
RATE_LIMIT_API_WINDOW=60
